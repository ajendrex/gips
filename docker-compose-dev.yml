version: "3"

services:
    db:
        volumes:
            - ./data:/var/lib/postgresql/data

    backend:
          volumes:
              - .:/app

    proxy:
        volumes:
          - ./conf/nginx-dev.conf:/etc/nginx/nginx.conf
